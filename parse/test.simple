function foo(i:int, j:int, k:int) : int
		{
			var total : int;
			var i : int;
		var m, n : bool;

		var o : intarray[100];
		var a, b : intarray[100];
			
			function square(t:int) : int 
			{
				var temp : int;
				temp = t * t;
				return 0;
			}
			
			total = 1;
			total=2;

			return 0;
		}